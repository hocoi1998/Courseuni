<div class="container">
    <form method="POST" action="/admin/courses/{{ courseSlug }}/{{ lessionSlug }}/handle-form-actions" name="container-form" class="mt-4">
    <h2>Quản lý bài tập</h2>
    <div>
        <a href="/admin/trash/courses/{{ courseSlug }}/{{ lessionSlug }}">Thùng rác ({{ deletedCount }})</a>
        <div class="mt-4 d-flex align-items-center">
            <div class="form-check">
                <input class="form-check-input" type="checkbox" value="" id="checkbox-all">
                <label class="form-check-label" for="checkbox-all">
                    Chọn tất cả
                </label>
            </div>

            <select name="action" class="form-control form-control-sm checkbox-select-all-options" required>
                <option value="">-- Hành động --</option>
                <option value="delete">Xoá</option>
            </select>
            <button class="btn btn-primary ml-4 check-all-submit-btn" disabled>Thực hiện</button>
        </div>
    </div>
    <table class="table mt-4">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">#</th>
                <th scope="col">Tên bài tập {{{ sortable 'name' _sort }}}</th>
                <th scope="col">Câu hỏi {{{ sortable 'question' _sort }}}</th>
                <th scope="col">Thời gian tạo {{{ sortable 'createdAt' _sort }}}</th>
                <th scope="col">Cập nhật cuối  {{{ sortable 'updatedAt' _sort }}}</th>
                <th scope="col">
                    <a href="/admin/courses/{{ courseSlug }}/{{ lessionSlug }}/create" class="btn btn-primary">Thêm bài tập</a>
                </th>
            </tr>
        </thead>
        <tbody>
            {{#each exercises}}
            <tr>
                <td>
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" name="checkSlugs[]" value="{{ this.slug }}">
                    </div>
                </td>
                <th scope="row">{{sum @index 1}}</th>
                <td>{{this.name}}</td>
                <td>{{this.question}}</td>
                <td>{{this.createdAt}}</td>
                <td>{{this.updatedAt}}</td>
                <td>
                    <a href="{{ lessionSlug }}/{{ this.slug }}/edit" class="btn btn-success">Sửa</a>
                    <a href="" class="btn btn-danger" data-toggle="modal" data-target="#delete-modal"
                        data-slug="{{ this.slug }}">Xoá</a>
                </td>
            </tr>
            {{else}}
            <tr>
                <td colspan="6" class="text-center">
                    Chưa có bài tập nào. <a href="/admin/courses/{{ courseSlug }}/{{ lessionSlug }}/create">Thêm ngay</a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</form>

{{!-- Confirm delete --}}
<div id="delete-modal" class="modal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Xoá bài tập</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Bạn chắc chắn muốn xoá bài tập này?</p>
            </div>
            <div class="modal-footer">
                <button id="btn-delete" type="button" class="btn btn-danger">Xoá bỏ</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Huỷ</button>
            </div>
        </div>
    </div>
</div>

{{!-- Delete hidden form --}}
<form id="delete-form" method="POST"></form>

<script>

    document.addEventListener('DOMContentLoaded', function () {
        var slug;
        var btnDelete = document.getElementById('btn-delete');
        var containerForm = $['form[name="container-form"'];
        var deleteForm = document.getElementById('delete-form');
        var checkboxAll = $('#checkbox-all');
        var itemCheckbox = $('input[name="checkSlugs[]"]');
        var checkAllSubmitBtn = $('.check-all-submit-btn');

        // When dialog confirm clicked
        $('#delete-modal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget)
            slug = button.data('slug')
        });

        // When delete button clicked
        btnDelete.onclick = function () {
            deleteForm.action = `/admin/courses/{{ courseSlug }}/{{ lessionSlug }}/${slug}/delete?_method=DELETE`;
            deleteForm.submit();
        }

        // Checkbox all changed
        checkboxAll.change(function () {
            var isCheckedAll = $(this).prop('checked');
            itemCheckbox.prop('checked', isCheckedAll);
            renderCheckAllSubmitBtn();
        });

        // item checkbox changed
        itemCheckbox.change(function () {
            var isCheckedAll = itemCheckbox.length === $('input[name="checkSlugs[]"]:checked').length;
            checkboxAll.prop('checked', isCheckedAll);
            renderCheckAllSubmitBtn().click(function (e) {
                e.preventDefault();
            });
        });

        // Re-render check all submit button
        function renderCheckAllSubmitBtn() {
            var checkedCount = $('input[name="checkSlugs[]"]:checked').length;
            if (checkedCount) {
                checkAllSubmitBtn.attr('disabled', false);
            } else {
                checkAllSubmitBtn.attr('disabled', true);
            }
        }

    });


</script>
</div>