<div class="container">
  <div class="row">
  <div class="col-8">
    gioi thieu
  </div>
  <div class="col-4">

    <table class="table">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">First</th>
        </tr>
      </thead>
      <tbody>
        {{#each lessions}}
        <tr>
          <th scope="row">{{ this.videoId }}</th>
          <td><a href="{{ this.courseSlug }}/{{ this.slug }}">{{ this.name }}</a></td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
</div>